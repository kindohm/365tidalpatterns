var environment = "development";
var config = {
  consoleLogLevel: "debug",
  environment: environment
};

config.express = {
	hostName: 'localhost',
	port: 9005,
	ip: '127.0.0.1'
};

module.exports = config;
